#include "main.h"
/**
 *_abs - computes the absolute value of number
 *@n: number to be computed
 *Return: 1 the absolute value of the integer
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
