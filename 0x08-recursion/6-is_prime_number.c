#include "main.h"
/**
 *is_prime_number - chexks if number is a prime number
 *@n: the number.
 *Return: 0 if not prime return 1
 */
int is_prime_number(int n)
{
if (2 % n == 0)
return (0);
else if (n < 0)
return (0)
else
return (1);
}
