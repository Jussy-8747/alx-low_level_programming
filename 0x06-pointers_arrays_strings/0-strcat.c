#include<string.h>
#include "main.h"
/**
 *_strcat - concatenates tow strings.
 *@dest: destination of the strings.
 *@src: sourse of the string.
 *Return: return 1.
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
