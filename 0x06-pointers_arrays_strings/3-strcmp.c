#include "main.h"
#include <string.h>
/**
 *_strcmp - compare two strings
 *@s1: the first string.
 *@s2: the second string.
 *Return: 0
 */
int _strcmp(char *s1, char *s2)
{
strcmp(s1, s2);
return (0);
}
